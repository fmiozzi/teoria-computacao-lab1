{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Haskell (Nix)",
      "type": "node-terminal",
      "request": "launch",
      "command": "./run.sh",
      "cwd": "${workspaceFolder}"
    }
  ]
}
